server:
  port: 8080

spring:
  # ==========================================
  # OPTION A: LOCAL H2 DATABASE (Commented out)
  # ==========================================
  # datasource:
  #   url: jdbc:h2:mem:tontracdb;DB_CLOSE_DELAY=-1
  #   driver-class-name: org.h2.Driver
  #   username: sa
  #   password: 
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console
  #     settings:
  #       web-allow-others: true
  # jpa:
  #   show-sql: true
  #   hibernate:
  #     ddl-auto: update
  #   properties:
  #     hibernate:
  #       dialect: org.hibernate.dialect.H2Dialect
  #       format_sql: true

  # ==========================================
  # OPTION B: SUPABASE POSTGRESQL DATABASE (Active)
  # ==========================================
  datasource:
    url: jdbc:postgresql://db.mzspyujraxtveqdyhuei.supabase.co:5432/postgres
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: WeighTruckpassword123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
